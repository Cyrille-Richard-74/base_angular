import { ApiEndPoint } from './api-endpoint.interceptor';

describe('ApiEndPoint', () => {
    it('should create an instance', () => {
        expect(new ApiEndPoint()).toBeTruthy();
    });
});
